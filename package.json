{
  "name": "ticket-bot-publisher",
  "version": "1.0.0",
  "type": "module",

  /* ▼ 追加：Node を 20 系で固定（22 系は一部 pkg が未対応） */
  "engines": {
    "node": "20.x"
  },

  "scripts": {
    "start": "node index.js",
    "deploy": "node commands/deploy-commands.js",
    "postinstall": "node commands/deploy-commands.js"   /* ← 変更なし */
  },

  "dependencies": {
    "discord.js": "^14.16.0",
    "node-fetch": "^3.3.2",
    "axios": "^1.7.2",
    "openai": "^4.30.0",
    "webdav": "^3.0.0",
    "google-spreadsheet": "^3.3.0"
  }
}
